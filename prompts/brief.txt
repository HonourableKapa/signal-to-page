You are an expert B2B content strategist. Based on the keyword, intent data, and competitive research below, produce a structured content brief.

Target keyword: {{KEYWORD}}

Intent and buyer context:
{{INTENT}}

Competitive research:
{{RESEARCH}}

Return a JSON object with exactly these fields:
- target_keyword: string — the exact target keyword
- intent: string — the intent_type from the intent data
- buyer_persona: string — the buyer persona from the intent data
- recommended_word_count: integer — based on avg_word_count from research, rounded to nearest 200
- competitor_urls: array of strings — top 5 URLs from serp_results
- paa_questions: array of strings — all PAA questions from the research
- content_gaps: array of strings — all content gaps from the research
- suggested_title: string — an SEO-optimised H1 that includes the target keyword naturally

Return only valid JSON. No explanation, no markdown fences, no commentary.
